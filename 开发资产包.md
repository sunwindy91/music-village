# 小有可为 · 开发资产包（给新窗口 agent 直接实现）

> 2026-08-12 · 配合 `0到1完整设计方案.md` 使用。这份是**可执行细节**：机制、判题算法、数据结构、生图提示词、演示脚本。
> 目标：8/13 初赛 · P0 三件套 + 尽力 P1 · 单人精工。

---

## A. 「音之梯」听辨关卡机制（P0 · 真实乐理核心）

### A1 · 关卡 1：谁更高（高低听辨）
- **发声**：Tone.js 合成两个音（间隔 0.8s），音域 C4-G4 内，两音跨度 ≥2 个半音
- **交互**：孩子点"高/低"两个大按钮（或拖动晓声上/下）
- **判题**：比较两音 MIDI 值（tonal.note( note ).midi）大小即可——**判题极简单、零误差**
- **反馈**：晓声"对啦！高的是这只鸟，因为它飞得高"；错了"再听一次，耳朵竖起来哦"
- **参数**：正确 3 次通关，随关数提高难度（音高更近：跨度 7→5→3 半音）

### A2 · 关卡 2：小鼓手（长短/节奏）
- **发声**：播放"走——走——走——停"节奏序列（四分音符=走，休止=停），用 Tone.Transport + Tone.Sequence
- **交互**：孩子跟着拍打（屏幕大鼓面，或空格/任意触摸）
- **判题（初赛简化版）**：不判精确节奏，判"是否在强拍上拍打"——监听 tap 时间戳与节拍网格的偏差 <250ms 记为命中；命中率 ≥60% 过关
- **进阶**：加"跑跑跑"（八分音符）

### A3 · 关卡 3：听音找家（同音/不同音，P1 可选）
- 播两个音 → 孩子判断"一样/不一样"→ 同音进窝
- 判题：tonal.note(...).midi 相等即可

### 判题核心（tonal 极简，全自动）
```js
import { note as n } from 'tonal';
const m1 = n('C4').midi, m2 = n('E4').midi;   // 60 vs 64
// 高低：m2 > m1 就是"更高"；同异：m1 === m2
```

## B. 网格作曲 + 五线谱出口（P0 · 最大差异化）

### B1 · 数据模型（网格 = 未来五线谱）
```js
// 网格：行 = 音高（低→高，如 C4..A4），列 = 拍（如 8 拍）
const piece = {
  notes: [ { pitch: 60, start: 0, dur: 2 }, { pitch: 64, start: 2, dur: 1 } ], // MIDI + 拍
  bpm: 90
};
```
- 点格子 = 加入 note；再点 = 删除；"播放"用 Tone.Sequence 按 start/dur 发声
- 音高行数：初赛 5 行（C D E F G），够孩子玩出旋律

### B2 · 五线谱出口（VexFlow 渲染）
```js
// 引入 VexFlow（CDN）：https://cdn.jsdelivr.net/npm/vexflow@4/build/cjs/vexflow.js
const { Renderer, Stave, StaveNote } = Vex.Flow;
const renderer = new Renderer(el, Renderer.Backends.SVG);
// 把 piece.notes 转成 StaveNote：pitch->key（tonal.note(midi).name），dur->duration（2=half,1=quarter,4=eighth）
// renderer 绘制五线谱 + 音符 → 孩子看到"我的歌变成真谱子了！"
```
- **关键转换**：`tonal.note(60).name` = "C4" → VexFlow key；dur 2→'h'、1→'q'、0.5→'8'
- 附"导出"按钮：下载谱子 PNG / 导出 MIDI（可选 P1）

### B3 · 音乐人格（P1 · 创作元素）
- 统计孩子的音符序列特征：跳进多（轻快）→小鸟型；级进多（平稳流动）→小溪型；高音多（闪耀）→星星型
- 展示人格卡片 + 愿景文案："学下去，你也能编出这样的歌"

## C. 叔叔大纲 → 关卡转化表（L1 主线，P1 做第 1 阶）

| 叔叔大纲 | 关卡玩法 | 乐理点 |
|---|---|---|
| ① 认识音符 1-7 | 音符变山果，听音摘果（发音→点对应果） | 唱名+音高对应 |
| ② 时值（四/二/全） | "走/停"身体化：音符多长，小人走几步 | 时值长短感知 |
| ③ 简单旋律弹奏 | 网格点 3 音旋律 → 五线谱渲染 | 视谱启蒙 |
| ④ 八分/十六/附点 | 节奏接龙：听鼓点排卡片 | 细分时值 |
| ⑤ 音程 | 音程梯子：两音距离几步 | 音程概念 |
| ⑥ 和弦 | 彩虹和弦：三色同时亮=和谐 | 和弦/级数（决赛） |

## D. gpt 生图提示词包（OpenNext 可用 · 用户执行）

> 统一风格锚点（每张开头都带）：`水彩插画，晨雾山谷，低饱和莫兰迪（雾蓝#bde4ff/晨金#e8b04b/森林绿#3e7d5e），大留白，治愈，高级感，无文字`

1. **晓声主形象**（站立引导）：
```
【风格锚点】圆润可爱的小山灵精灵，青绿色圆润身体，头顶一片嫩芽叶，眼睛亮晶晶，
双手背在身后微微歪头，像在邀请小朋友，水彩质感，单一角色全身，透明/纯色底
```
2. **山谷主场景**（地图背景）：
```
【风格锚点】清晨山谷全景，五线谱化作起伏的山脊线延伸向远方，彩色音符像萤火虫
飘浮在山谷间，一条发光的小路通向山上的木屋学校，太阳在晨雾中升起
```
3. **音符萤火**（可点引导元素，3 个）：圆形发光音符（全音符/四分音符/八分音符），柔和光晕，透明底
4. **听音关场景**（谁更高）：山谷中两只不同高度的小鸟，站得高的在更高的山岩上
5. **五线谱山脊卡**（成就感展示）：一张泛黄卷轴，上面是用水彩画的五线谱，音符正在发光
6. **音乐人格三卡**（小鸟/小溪/星星）：分别配水彩小鸟/溪流/星星的小图标卡

## E. 3 分钟演示视频脚本

| 时间 | 画面 | 旁白 |
|---|---|---|
| 0:00-0:20 | 山谷开场+晓声 | "没有音乐老师的山里，孩子也能学音乐" |
| 0:20-0:55 | 「音之梯」谁更高 | "真实音高听辨——不是认图形，是练耳朵" |
| 0:55-1:35 | 网格作曲 | "孩子把音符当种子，种出旋律" |
| 1:35-2:10 | **五线谱出口** | "一键变成真谱子——这可能是他人生第一首谱" |
| 2:10-2:40 | 音乐人格+积分 | "每个孩子都有自己的音乐人格" |
| 2:40-3:00 | 愿景 | "学下去，你也能编出这样的歌" |

## F. 技术集成要点（CDN，无构建也行）

- **Tone.js**：`https://cdn.jsdelivr.net/npm/tone@14/build/Tone.js`
- **tonal**：`https://cdn.jsdelivr.net/npm/tonal/browser/tonal.min.js`
- **VexFlow**：`https://cdn.jsdelivr.net/npm/vexflow@4/build/cjs/vexflow.js`
- 麦克风判题（P1 唱关）：`navigator.mediaDevices.getUserMedia` + 分析器（来时路乐感检测已实现过 YIN，可复用思路）
- **纯前端可跑 + 可部署 Pages**（低配设备友好，零后端）

## G. 验收清单（新窗口 agent 完成自查）

- [ ] P0：山谷场景 + 晓声 + 听音关（能真判题）+ 网格作曲（能播放）+ 五线谱出口（能渲染）
- [ ] P1 尽力：认识音符关 + 音乐人格 + 积分
- [ ] Impeccable `/audit` 通过（对比度/触控 44px/动效 transform）
- [ ] 部署 music-village-app 可在线演示
