# 大山里的音乐课 · 验收交接包（2026-08-12）

> 用途：给**新窗口 agent** 做验收用。本文件 = ① 已完成内容清单 ② 全程对话/决策流水 ③ 可复现验证方法 ④ 验收清单 ⑤ 已知问题与后续。
> 主窗口已完成从零精工实现（P0+P1 全量），冒烟测试 **27 项断言全 PASS、无运行时错误**。

---

## 一、项目背景（一句话）

**小有可为 · AI 向善创新挑战赛 · 乡村教育赛道 · 初赛 8/13** —— 给山区 6-9 岁零基础儿童的 AI 陪伴式乐理寻宝游戏（无账号、零收集、纯前端、离线可用）。

蓝本文档（已实现，非仅参考）：
- `0到1完整设计方案.md`（真实乐理内核 + 三大教学法 + Chrome Music Lab 机制 + 五线谱出口）
- `开发资产包.md`（判题算法 / 数据结构 / 生图提示词）
- `新窗口开工包.md`（设计宪法 / 质量红线）
- `docs/DESIGN.md`（设计系统宪法）

**实现原则**：不参考旧 `src/` 的低幼实现，从零精工重写。

---

## 二、已完成内容（逐项）

### P0 · 三件套（全部完成 ✅）

| 块 | 内容 | 关键实现 |
|---|---|---|
| **① 山谷主场景 + 晓声** | Splash 开场 / 水彩山谷地图 / 地点总览面板 / 晓声山灵 | 全手写 SVG：晓声（表情系统 calm/happy/thinking/surprised/encourage + 打字机气泡 + 萤火粒子 + 成长印记）；地图（山脊=五线谱意象、寻宝小路、溪水、云、太阳） |
| **② 音之梯 · 真实听辨** | 谁更高 / 小鼓手 / 听音找家 三关 | **真实音高判题**：tonal MIDI 比较（零误差）；小鼓手用 Tone Transport 拍点绝对时间 + ≤250ms 窗口判拍（命中率 ≥60% 过关）；难度递进（跨度 7→5→3 半音） |
| **③ 网格作曲 + 五线谱出口** | 旋律田 5×8 网格 / 4 音色 / 变谱 / PNG·MIDI 导出 | Chrome Music Lab Song Maker 式；**VexFlow 渲染真实五线谱**（SMOKE 测试已验证真实 SVG 输出）；PNG 保存 + MIDI 导出（手写 MIDI Format 0） |

### P1 ·（全部完成 ✅）

| 块 | 内容 |
|---|---|
| **认识音符** | 听音摘果，唱名+简谱；**渐进解锁**：do re mi → 加 fa sol → 全 1-7；答错报正确答案 + 再播该音（学习闭环） |
| **音乐人格** | 分析旋律特征自动归类：跳进多→小鸟型 / 上行+宽音域→星星型 / 级进→小溪型；变谱时弹人格卡 + 愿景文案 |
| **积分闭环** | 答对 +10 / 通关 +50 / 作曲 +30；顶栏积分 pill + 飘字动画 + localStorage 持久化 + 「我的音乐会」作品墙 |

### 三层课程结构（循序渐进已落地）

```
L0 听觉启蒙（声音山谷）：谁更高 → 小鼓手 → 听音找家
L1 乐理进阶（音阶山谷）：认识音符（do-re-mi → 7 音渐进）
L2 创造表达（旋律草原）：旋律田作曲 → 五线谱出口 → 音乐人格
```

### 技术栈（全部本地化，离线可用）

| 库 | 位置 | 用途 |
|---|---|---|
| tone.js 14.9.17 | `src/vendor/tone.js` | 合成器/Transport/节奏 |
| tonal.min.js | `src/vendor/tonal.min.js` | 乐理判题（MIDI/音名） |
| vexflow.js 4.2.5 | `src/vendor/vexflow.js` | 五线谱渲染 |
| jsdom（仅测试） | `src/node_modules` | 冒烟测试 |

### 文件结构

```
music-village/src/
├── index.html              页面骨架（Splash/地图/关卡/庆祝/音乐会/五线谱面板）
├── config.js               全局配置 + MV.Tap 事件工具 + pointerdown→click 兼容层
├── app.js                  状态机/路由/积分/5 个关卡执行器/庆祝/音乐会
├── styles/main.css         设计系统（晨雾水彩 + Chrome Music Lab 极简几何）
├── shared/
│   ├── levels.js           内容层：3 地点 + 5 关卡模板 + solfa + gridPitches
│   └── lines.js            晓声台词层（孩子话·不说教）
├── modules/
│   ├── audio/music-core.js Tone 引擎（温柔音色·节奏·音效·上下文自保）
│   ├── voice/voice-core.js 晓声 SVG/打字机/表情/萤火/成长
│   └── notation/staff.js   VexFlow 渲染 + PNG/MIDI 导出
├── vendor/                 三个本地库（离线可用）
└── tools/smoke.js          冒烟测试（jsdom，27 项断言）
```

---

## 三、全程对话 / 决策流水（按时间线）

### 阶段 1 · 读方案与准备
1. 用户要求：读 `0到1完整设计方案.md`+`开发资产包.md`+`新窗口开工包.md`，**从零精工实现**，P0 三件套优先、P1 尽力、不参考旧 src、每块跑 Impeccable audit。
2. 通读三文档 + DESIGN.md，确认技术选型（tonal/Tone.js/VexFlow，本地 vendor 优先，山区无网也能跑）。
3. 清空旧 `src/`（旧实现是"小鸟对谱子"低幼版，全部弃用），建新目录骨架。
4. 下载三个库到 `vendor/`（离线可用）。

### 阶段 2 · 从零实现（P0-1 → P0-2 → P0-3 → P1）
5. 建基础层：`config.js` / `shared/lines.js` / `shared/levels.js` / `styles/main.css`（DESIGN.md 宪法落地）。
6. 建 `voice-core.js`（晓声 SVG 山灵 + 打字机 + 表情 + 萤火 + 成长印记）。
7. 建 `music-core.js`（Tone.js 温柔音色 + 听辨发声 + 节奏调度 + 音效）。
8. 建 `app.js` 核心（状态机/路由/积分/Splash/地图/关卡分派）+ `staff.js`（VexFlow + PNG/MIDI 导出）+ `index.html`。
9. 实现 P0-2 三个真实听辨关卡（谁更高/小鼓手/听音找家）。
10. 实现 P0-3 网格作曲（含五线谱出口 + 音乐人格 + 积分）与 P1 认识音符。

### 阶段 3 · 冒烟测试与重大 Bug 修复（本轮最重要的产出）
11. 用户开梯子后网络恢复 → `npm i jsdom` 成功，开始运行时冒烟测试。
12. **Bug A · 关卡会话令牌失效（真实产品 bug）**：
    - 症状：进入新关卡时 `startLevel` 永不执行（trace 证明旧关卡的 say/播放链抢占全局单气泡 VoiceCore）。
    - 根因：离开关卡（stage-back/celebrate/openConcert）时 `MV._session` 未失效，遗留 setTimeout 链继续跑并无条件 `say()` 抢气泡。
    - 修复：`invalidateSession()` 在 openLevel/stage-back/celebrate/openConcert 调用 + `playRound` 入口 `alive()` 守卫 + 补齐未守卫的 setTimeout。
13. **Bug B · 测试脚手架 String.replace `$` 转义陷阱**：
    - 症状：jsdom 报 `Identifier '$' has already been declared`，大量时间花在二分定位（最终定位到：`html.replace('</body>', inline + '</body>')` 字符串替换会把 `$$` 当转义符吞成 `$`，损坏 app.js 的 `const $$`）。
    - 修复：改用**函数替换** `() => inline + '</body>'`。
    - 教训：拼 HTML 一律用函数替换，避免 `$` 模式。
14. jsdom `window.eval` 下 "use strict" 的 `var` 不落 window（Tonal 假性缺失）→ 改用 `runScripts:'dangerously'` + 内联 `<script>` 元素（真实脚本语义）。
15. 冒烟测试 27 项断言全 PASS、无运行时错误（含真实 VexFlow 渲染 SVG 断言）。

### 阶段 4 · 浏览器实测与用户反馈修复
16. 用户问"网页在哪儿" → 启动本地服务器（python http.server）。
17. 404 排查：服务器根目录问题 → 改用 `--directory` 明确指定 `src` 为根。
18. **Bug C · 打开只见一层虚化背景（真实产品 bug）**：
    - 根因：`.view` 默认 `display:none`，启动时从未调用 `showView('splash')`，整个 Splash（晓声/按钮）被隐藏，只剩 body 渐变背景。
    - 修复：`boot()` 里加 `showView('splash')`；并新增断言"开场视图已激活"防回归。
19. **Bug D · 按钮点了没反应**：
    - 修复 ①：`go()` 里音频初始化加 try/catch（Tone 异常不能卡住进入地图）。
    - 修复 ②：`config.js` 加全局兼容层——不支持 PointerEvent 的环境自动把 `pointerdown` 降级为 `click`（不双触发，已断言验证）。
20. **Bug E · 声音没出来**：
    - 根因推断：`init()` 任一步抛错 → `ready=false` → 全程静音且错误被吞。
    - 修复：音频引擎**逐音色独立 try/catch**（主音色优先保证）+ 每次发声前 `ensureRunning()`（Tone.start 幂等解锁上下文）+ 进入地图的"欢迎音"自检 + `[MusicCore]` 错误日志。
    - 用户侧提示：用系统浏览器（Chrome/Edge）打开，勿用 VS Code 内置浏览器（音频常被禁用）。

### 阶段 5 · 设计溯源与 gpt 生图验证
21. 用户问设计灵感来源 → 检索 DESIGN.md/产品思维导图/方案（Spiritfarer·Bloom·Chrome Music Lab·三大教学法·叔叔大纲）。
22. 用户问是否初始课程/循序渐进 → 已确认并给出 4 层递进。
23. 用户问美工是否用 gpt + 500 刀密钥 → **实测确认**：
    - 美工当前**未用 gpt**（全手写 SVG）。
    - **openai-next 500 刀额度可用**：读取 `~/.codex/auth.json` 密钥（不显示值），调 `https://api.openai-next.com/v1/images/generations`，**dall-e-3 + 1024x1024 → HTTP 200 成功出图**；`gpt-image-1` 不支持（unknown_model）。

---

## 四、可复现验证方法（给验收 agent）

### 1. 语法校验
```powershell
cd C:\Users\23017\Desktop\AI比赛\music-village\src
node --check app.js; node --check config.js; node --check shared/lines.js
node --check shared/levels.js; node --check modules/audio/music-core.js
node --check modules/voice/voice-core.js; node --check modules/notation/staff.js
node --check tools/smoke.js
```

### 2. 冒烟测试（jsdom 运行时全流程）
```powershell
# 需先安装依赖（已装）：npm i jsdom
node tools/smoke.js
# 期望：27 项断言全 PASS，结尾 "✔ 无运行时错误"
```
覆盖：Splash 激活→地图 3 节点→5 个关卡 startLevel→作曲 40 格→种/清/变谱→积分+30→人格卡→五线谱 SVG 真实渲染→事件兼容层（有/无 PointerEvent 两路径）。

### 3. 本地试玩
```powershell
python -m http.server 8765 --bind 127.0.0.1 --directory "C:\Users\23017\Desktop\AI比赛\music-village\src"
# 浏览器打开 http://127.0.0.1:8765/index.html
```

### 4. 人工体验路径
Splash（点开始）→ 声音山谷（谁更高/小鼓手/听音找家）→ 音阶山谷（认识音符）→ 旋律草原（网格作曲→**变成五线谱**→保存图片/存 MIDI）→ 我的音乐会 → 返回地图看点亮状态 + 积分。

---

## 五、验收清单（新窗口 agent 逐项打勾）

- [ ] 语法校验全部通过（见上）
- [ ] `node tools/smoke.js` 27 项全 PASS、无运行时错误
- [ ] 打开页面能看到 Splash（晓声 + 开始按钮），**不是虚化背景**
- [ ] 按钮可点（开始寻宝、跳过、关卡 chip、返回、关闭）
- [ ] 有声（进入地图听到"do"欢迎音；听辨关有音效）——**用系统浏览器**
- [ ] 谁更高能判题（答对/答错反馈、3 题过关、跨度递进）
- [ ] 小鼓手节奏判拍（拍点窗口 ≤250ms）
- [ ] 认识音符渐进解锁（首轮 3 果）
- [ ] 网格作曲可种/播/清，变谱出真实五线谱，作品进音乐会，人格卡弹出，积分增加
- [ ] 刷新后进度保留（localStorage）
- [ ] 移动端/窄屏不破版（网格横向滚动、触控目标 ≥44px）

---

## 六、已知问题 / 注意事项

1. **smoke 测试"小鼓手"曾有偶发时序抖动**（等待时长问题，非产品 bug；已把等待加长到 650ms，偶发可重跑确认）。
2. **gpt 生图**：openai-next 仅支持 `dall-e-3`（标准尺寸 1024x1024 等）；`gpt-image-1` 不可用。
3. **CDN 图片 URL 下载慢**：生图返回的是 `pro.filesystem.site` 链接，本地下载偶发超时（建议直接存 base64 或换下载时机）。
4. **VS Code 内置浏览器（Simple Browser）音频可能被禁用**：演示请用 Chrome/Edge。
5. **未接 LLM 对话/哼唱识别**（设计文档明确初赛不做，决赛再做）。
6. 旧 `src/` 已全部清除重写（git 可看到大 diff）；旧测试文件 test-audio.html / test-voice.html 已删。

---

## 七、后续推进计划（供新窗口参考）

- **P0（今天）**：部署可试玩链接（`wrangler pages deploy src --project-name music-village-app`）；整理 3 分钟演示脚本（模板在 `docs/演示视频脚本_3分钟.md`）。
- **P1（有余力）**：用已验证的 dall-e-3 通路批量生成 6 组水彩素材（提示词在 `开发资产包.md` D 节），替换/叠加到界面（晓声 PNG、山谷背景、五线谱山脊卡、人格三卡）。
- **P2**：L1 第 2 阶（时值"走/停"）、难度曲线、方向材料/答辩口径。
- **红线**：宁可少功能不要糙功能；改 UI 前跑 Impeccable audit；动效全部 transform/opacity + prefers-reduced-motion。

---

## 八、当日改动流水（8/12 午后 · 主窗口）★ 新窗口必读

### 视觉/性能
- **素材全部转 WebP**（18MB → 1.2MB）：	ools/webp_convert.py 批量压缩，所有引用已切 .webp（valley/xiaosheng/scroll/level_pitch/person_*/note_*）。加载不再转圈。
- **地图水彩修复**：.map-scene 背景改为 alley.webp（原旧 SVG 天空被它盖住导致"丑画面"）；旧 SVG 仅保留寻宝小路 #trail 叠加，其余视觉元素 display:none。关卡节点/晓声/音乐会按钮全部保留。
- **关卡内 UI 手绘水彩统一**：stage-panel 羊皮纸底（scroll.webp）+ 手绘内虚线框 + 不规则圆角；nswer-btn 从扁平纯色改为奶油纸底+水彩描边+深色文字（对比度 ≥4.5:1）；fruit/chip/step-dot/grid/nv-label 全部纸感统一；听音三关加 level_pitch.webp 场景淡叠。

### 晓声 6 阶段成长形态（★ 核心新系统）
`
阶段 点亮数   key     名称   素材
1     0      seed    种子   xiaosheng.webp
2     1      sprout  小芽   xiaosheng_sprout.webp
3     2      bloom   开花   xiaosheng_bloom.webp
4     3      spark   星光   xiaosheng_spark.webp
5     4      fawn    初鹿   xiaosheng_fawn.webp
6     5      deer    鹿     xiaosheng_deer.webp（全部点亮=终极形态🦌）
`
- 实现：oice-core.js FORMS 数组 + pplyGrowth(stage) 按点亮数选形态（Image() 预加载，缺图自动保持当前形态不报错）；地图晓声脚下 .xs-form-tag 显示"形态名 · 点亮 n/5"；stageNum() 封顶 5。
- **蜕变动画**：形态切换有 orm-in 形变；切到 fawn/deer 触发 .xs-burst 金色进化粒子爆发。

### 课堂陪伴（教学法落地）
- 卡关陪伴：谁更高/听音找家/认识音符 三关连续错 2 次 → 晓声安抚话术 + 降难度重播（lines.stumble），不判死。
- 成长仪式：首次通关 lines.grow.lit"你点亮了这座山…"；再通关 grow.spark"金光飞过来了…"。

### 通关主题曲框架
- config.themeSong：占位旋律（C 大调五声音阶，{midi,start,dur}），全部 5 关点亮后 celebrate 自动播放 + 显示歌名；lyrics 留空=不显示歌词区。**旋律/歌词由用户自己谱**（气质参考 country road / Let It Go / 鹿 befree）。

### 部署
- 当前最新：wrangler pages deploy src --project-name music-village-app（每次新 hash）
- 冒烟：
ode tools/smoke.js = 27 PASS / 0 FAIL / 0 onerror
- 本地实测：$env:TEMP\xs_test\test.js（jsdom 加载全站，确认 loc-node=3 / footer / form-tag 正常）

### 已知 & 注意
- 5 张形态素材（sprout/bloom/spark/fawn/deer）由 dall-e-3 生成中，生成后需 python tools/webp_convert.py 转 webp 再部署。
- 形态缺图时自动保持当前形态（防 404），素材到位即自动切换。
