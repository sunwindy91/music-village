# Pro 审查报告 · 2026-08-13（初赛截止日）

> 审查人：导演（DeepSeek V4 Pro）· 范围：全产品功能 + 课程体系 vs 初赛需求 + 材料口径
> 版本基线：`d54091a`（封面焕新后）· 冒烟 52 PASS / 0 FAIL / 0 onerror

---

## 一、结论一句话

**产品功能维度远超初赛要求（A+）；最大风险不是功能，而是「材料口径滞后」和「演示视频未录」**——今天 8/13 截止，这两项必须优先补齐。

---

## 二、课程体系评析（客观 · 逐项 vs 初赛需求）

初赛要求（据赛事材料）：可试玩 MVP + 3 分钟演示视频 + 方向材料。

| 维度 | 现状 | 评价 | 证据 |
|---|---|---|---|
| **可玩原型** | 5 聚类 21 关全可玩、全真实判题 | ✅ 远超要求 | 冒烟 52 项断言覆盖全部关卡启动 |
| **教育性** | 听觉(L0)→节奏→乐理(L1,对齐叔叔大纲)→和声→创造(L2) 五段递进；每聚类「理论导入→关卡练习→乐理小测」闭环 | ✅ 优 | `聚类课程架构_v1.md` + levels.js 课程编号 |
| **陪伴智能** | 3D 晓声 6 形态成长 + 5 类情绪姿态换图 + TTS 真实语音 + 主动引导下一关 + 卡关不判死 | ✅ 突出亮点 | F-07 姿态层 + edge-tts 16 条语音 |
| **创新性** | 晓声 AI 陪伴（成长/情绪/语音/引导）+ 认全机制 + 五线谱/MIDI 出口 | ✅ 有差异化 | 材料 v4 亮点 1-5 |
| **可及性** | 零账号零收集、纯前端离线、素材全 webp、低配友好 | ✅ 达标 | DESIGN.md 红线 |
| **风险 1** | 初赛方向材料 v3 口径严重滞后（写「3 地点 5 关 SVG 晓声」，实际 5 聚类 21 关 3D+TTS） | ❌ 必须修 | 见 §四清单 |
| **风险 2** | 答辩材料 v4 口径部分滞后（15 关/41 断言/SVG，实际 21 关/52 断言/3D+TTS） | ❌ 必须修 | 见 §四清单 |
| **风险 3** | 3 分钟演示视频未录制（F-04 脚本已备） | ❌ 今日必做 | `演示逐镜脚本.md` |

**评析结论**：教学闭环（教—学—评）成立、判题真实、陪伴有温度，**足以通过初赛**；材料与视频补齐后无硬伤。

---

## 三、bug / 风险清单

### 已修复（本轮及之前）
1. 晓声形态按「累计关卡数」→ 5 关即满级、第二关全盛 ❌→✅ 改聚类进度（种子→鹿 6 档）
2. 通关页漏 `applyGrowth` → 回种子图 ❌→✅ mountXs 统一收口
3. 首页虚线连线错位/奇怪 ❌→✅ 去虚线 + 序号徽章流程模式
4. `.xs-png` border-radius 裁切 3D 手办 ❌→✅ contain + 底对齐

### 遗留风险（可接受 / 待观察）
| # | 风险 | 影响 | 处置 |
|---|---|---|---|
| R1 | iOS Safari 自动播放限制：splash 欢迎语音可能静音 | 低（气泡文字兜底，catch 已接） | 已兜底，观察 |
| R2 | 地图 `nextStop` 引导与 `_pendingStory` 故事线同时触发时 say 覆盖 | 低（仅跨形态+聚类全亮同时） | 可接受 |
| R3 | splash hero 背景含 3D 晓声 + 内容区晓声（远近双晓声） | 视觉重复 | 观察用户反馈，可重生成无角色 hero |
| R4 | 音色关乐器图标（inst_*）若未生成 → onerror 兜底空图 | 低（有兜底） | 待确认素材 |
| R5 | 主题曲未谱（config.themeSong 空） | 中（5 聚类全亮无歌） | 用户谱好后填 |

---

## 四、今日必须行动（按序）

1. **更新材料口径**（导演已开始）：初赛方向材料 v3 + 答辩材料 v4 → 对齐「5 聚类 21 关 / 3D 晓声 + TTS + 姿态 / 52 断言」
2. **录制 3 分钟演示视频**（用户）：按 `演示逐镜脚本.md`（26 镜 177s），演示模式 `localStorage.setItem('mv-unlock-all','1')` 全开
3. **主题曲**（用户哼唱 → Suno 生成 → 填 `config.themeSong`）
4. **报名上传**：材料 + 视频 + 原型链接

---

## 五、8/13 下午 · 第二轮审查（AI 对话接通后）

### 新修复 / 新增
- ✅ **问问晓声**：地图按钮 + 点晓声本体（拖拽不误触）→ 对话盘；13 条本地规则问答
- ✅ **真实 AI 聊天**：Cloudflare Worker `music-village-chat`（DeepSeek，晓声人设 system prompt，1-3 句短答、孩子话、懂乐理、不否定），key 存 Worker secret 不进仓库；前端 8s 超时自动回落本地规则（离线可用）
- ✅ **语音双通道**：🎤 `webkitSpeechRecognition` 语音提问 + 🔊 `speechSynthesis` 中文播报回答
- ✅ **缓存 bug**：JS/CSS 引用加版本号（`?v=20260813f`），消灭"旧 JS 静默失效"
- ✅ 连线淡细虚线 + 沿节点下缘弧线 + z 降层（不破坏水彩主画面）
- ✅ 形态改每 3 关升一档（3 关小芽→6 开花→9 星光→12 初鹿→15 鹿）
- ✅ 冒烟 52 → **54 项断言**（新增问答盘 2 项），0 FAIL

### 遗留（低风险，已兜底）
| # | 项 | 状态 |
|---|---|---|
| R1 | iOS 自动播放限制（欢迎语音可能静音） | 气泡文字兜底 ✓ |
| R3 | splash hero 远景晓声 + 内容区近景晓声 | 用户未反馈，待观察 |
| R5 | 主题曲未谱 | 待用户谱好填 `config.themeSong` |

### 演示建议（视频录制时）
- 开场 → 点晓声对话盘 → **用🎤语音问「什么是音高」** → 晓声 AI 回答+播报 → 继续关卡
- 这段 15 秒就是「AI 陪伴」的最强实锤，建议录进 3 分钟视频

---

## 六、8/13 晚间 · 第三轮审查（课程严谨度 + 提交冲刺）

### 课程严谨度修复（知识诅咒）
- ✅ 解锁链改 **声音山谷 → 音阶山谷（认谱/时值主课）→ 节奏小路（十六分进阶）→ 和弦花园 → 旋律草原**，严格对齐叔叔大纲「时值→八分→十六分」
- ✅ 重依赖关卡 prereq 温习提示：看谱弹奏（需认识音符/音符住址/走走停停）、十六分赛跑（需时值+八分）、和声找朋友（需叠音+音程）——跳关进入时晓声先提示温习
- ✅ 全关卡知识依赖链逐关核对无倒置（见正文）
- ✅ 材料口径同步（v3.1/v4.1）

### 交互体验修复
- ✅ Splash 等点击进入（去自动跳转，录制可控）
- ✅ 种子形态情绪动效（无变体图时：答对跳/答错摇/卡关歪头/庆祝连跳）
- ✅ 全通关皮肤切换（点晓声标签 → 6 形态任意换）
- ✅ 连线亮度提高 + 虚线流动闪动动画
- ✅ 自然音色 TTS（Google 免费中文女声，Worker 代理）
- ✅ 进化仪式动画（跨形态通关：旧形态缩小淡出→金光爆→新形态弹出+上行音阶）

### 提交冲刺
- ✅ `docs/初赛提交冲刺包.md`（报名→录视频→交材料三步清单 + 项目介绍可直接复制）
- ✅ 演示脚本 v2（10 镜 170s，对齐完整版）
- ⏳ 报名意向表（用户填）
- ⏳ 演示视频（用户录，导演护航）
- ⏳ 提交（今晚 24:00 截止）

